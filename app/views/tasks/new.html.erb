<% if params[:project_id] %>
<h1>What's the next thing you can do to: "<%= Project.find(params[:project_id]).name %>"</h1>
<% else %>
<h1>Lets make a New Task!</h1>
<% end %>
<button><%= link_to "Cancel", projects_path %></button>

<%= render 'form' %>