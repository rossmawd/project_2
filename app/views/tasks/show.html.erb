<h1>Task: <%=@task.name.capitalize  %></h1>

<% if @task.tags == [] %>
  <h3>This Task has no Tags!</h3>
<% else %>
  <h3>Tags:</h3>
  <ul>
  <% @task.tags.each do |tag| %>
  <li><%= link_to( tag.name, tag_path(tag)) %></li>
  <% end %>
  </ul>
<% end %>